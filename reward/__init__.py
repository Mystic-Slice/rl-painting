"""reward/ -- rendering + reward components for the p5.brush RL loop.

Importable WITHOUT tinker_cookbook: only needs httpx, node, and the datagen
plumbing it reuses via `reward.compat`. The tinker integration lives in `train/`.
"""
